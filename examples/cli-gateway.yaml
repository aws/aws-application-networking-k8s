apiVersion: gateway.networking.k8s.io/v1beta1
kind: Gateway
metadata:
  name: cli-cluster-gateway
spec:
  gatewayClassName: amazon-vpc-lattice
  listeners:
  - name: http
    protocol: HTTP
    port: 80
