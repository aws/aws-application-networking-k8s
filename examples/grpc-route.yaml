apiVersion: gateway.networking.k8s.io/v1alpha2
kind: GRPCRoute
metadata:
  name: grpc
spec:
  parentRefs:
  - name: my-hotel
    sectionName: https
  rules:
  - backendRefs:
    - name: grpc-server
      kind: Service
      weight: 10
    matches:
        - method:
            service: foo
            method: bar
